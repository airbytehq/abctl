airbyte-bootloader:
    env_vars:
        PLATFORM_LOG_FORMAT: json
global:
    auth:
        enabled: true
    env_vars:
        AIRBYTE_INSTALLATION_ID: test-user
    jobs:
        resources:
            limits:
                cpu: "3"
                memory: 4Gi
    storage:
        type: local
